# 数据目录约定

本教程的示例优先使用程序生成数据或 scikit-learn 内置小数据集，因此无需下载文件。

真实研究项目建议使用：

```text
data/
├── raw/          # 原始数据，只读，永不手工修改
├── interim/      # 中间处理结果
├── processed/    # 可直接用于建模的数据
└── external/     # 外部来源数据
```

不要把大型数据、隐私数据或带许可证限制的数据提交到 Git。应记录数据版本、获取日期、来源、许可证、哈希值、字段含义、样本排除规则和已知偏差。
